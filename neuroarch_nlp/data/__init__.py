import defaults

# These default values are included for convenience.
# You'll want to dynamically load these and other values from your database(s).

colors_values = defaults.colors_values
transmitters = defaults.transmitters
localities = defaults.localities
synapticities = defaults.synapticities
ownerinstances = defaults.ownerinstances
othermods = defaults.othermods
